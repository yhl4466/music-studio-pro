# 智能音乐工坊 PRO

一个在浏览器里用的音乐制作工具。打开网页就能编曲、试听、导出，不需要安装任何软件。

在线使用：https://yhl4466.github.io/music-studio-pro/

首次部署需要等 1-2 分钟。

## 功能

**编曲**

- 多轨时间线（最长 128 小节），可以写旋律、贝斯、和弦、琶音和鼓组
- 在网格上点击或拖拽画音符，支持框选、复制、量化和连音细分

**AI 辅助生成**

- 内置 10 种风格（Lo-Fi、流行、EDM、Synthwave、摇滚、爵士、电影配乐、环境等）
- 选定风格和情绪参数后，一键生成完整的编曲草稿，再手动修改
- 支持单轨重写、风格迁移、动机续写

**音色**

- 6 种合成器音色（主音、拨弦、Pad、贝斯、铃音、风琴）
- 8 件鼓组（底鼓、军鼓、拍手、闭镲、开镲、通鼓、边击、吊镲）
- 全部用 Web Audio 实时合成，没有加载任何采样文件

**混音**

- 每轨独立控制音量、声像、混响和延迟发送
- 主输出带 3 段 EQ 和砖墙限制器

**导入导出**

- 导出 WAV 音频、MIDI 文件、工程 JSON
- 支持分享链接，别人打开链接就能还原你的工程

**其他**

- 主题：深色 / 浅色 / 跟随系统三档，底层保留 6 套配色
- 屏幕钢琴键盘，支持电脑键盘弹奏（A-L 白键，W/E/T/Y/U 黑键）
- 本地作品库，保存编过的曲子

## 🎨 音乐可视化

独立的可视化页面，从主应用菜单「▤ 可视化」进入（会把当前工程复制过去，两边数据同源）。

在线访问：https://yhl4466.github.io/music-studio-pro/visualizer.html

**8 个实时渲染器**（顶栏下拉切换）

- 实时波形 · 频谱瀑布 · 心电图 · 雷达图
- 径向频谱 · 粒子系统 · 跳动波形 · 3D 频谱森林

**3 种图片生成器**（「生成封面」弹窗内切换，都支持 2× 高清导出）

- 专辑封面（3 种风格：同心声纹 / 能量砖阵 / 波形缎带，800×800）
- 音乐指纹（1920×400，含缩略频谱 + 能量曲线 + 音高分布）
- 分享卡片（1200×630，含波形 + 关键数据 + 链接）

播放、拖动旋转、实时参数、渲染长度（30/60/90 秒 / 全曲）都在同一页里，不需要联网，也不上传任何数据。

## 关于 AI 作曲

这里的“AI”指的是一套基于音乐理论的规则化生成算法，不是神经网络或大语言模型。

生成过程大致是这样的：

- **和弦**：从风格预设的和弦走向里选，用声部进行代价函数挑选转位，避免声部大跳
- **旋律**：按 2 小节一个乐句生成，带有动机重复、模进、拱形力度、经过音和前倚音
- **节奏**：每个风格有独立的鼓组模板、贝斯律动和琶音模式
- **曲式**：按总小节数自动划分“引子—发展—高潮—尾声”，不同段落的密度和力度不同
- **种子**：所有随机性由种子驱动，同一种子生成相同结果，可以复现

这么设计的原因是：纯前端运行，没有服务器；生成逻辑都在代码里，可以查阅；点一下立刻出结果，不用等网络请求；生成的是草稿，你可以随意修改，AI 只是帮你起步。

**长曲支持**：AI 会按总小节数自动规划段落结构，不同段落用不同的密度和力度，不再是简单重复。整首曲子的旋律由一个核心动机发展而来（转调、倒影、节奏伸缩、片段借用），不是每句重新随机。

如果你的期待是“输入一句话，AI 帮我写出一首完整的歌”，这个工具做不到。它的定位是给你一个还不错的和弦进行和基础编曲，你在此基础上改。

## 使用建议

推荐在电脑上使用（Chrome、Edge、Safari 都可以）。大屏幕配鼠标，编辑体验最好。

手机暂不支持，因为布局会错乱，而且不易操作，平板可以用，但是也不太方便。

关于长曲：128 小节下生成音乐会有一段约 2.5 秒的界面冻结（同步建 DOM 的固有限制），生成完成后编辑与播放基本流畅，但滚动时可能略有顿挫。日常使用 32-64 小节完全流畅。

## 常见问题

**为什么本地双击 HTML 打不开？**

项目用了 ES modules，浏览器不允许 `file://` 协议加载。在线版没有这个限制，直接访问链接就行。本地测试请用 HTTP 服务，命令见下方。

**我的音乐存在哪里？**

全部存在你浏览器的 localStorage 里，不会上传到任何服务器。换浏览器或清缓存会丢失，建议用“导出工程文件”备份。

**支持哪些拍号？**

AI 作曲目前只支持 4/4。其他拍号下手动编辑正常，AI 生成会提示切回 4/4。

**生成的音乐能商用吗？**

可以。但 AI 只是起点，成品质量取决于你的修改。

## 🎨 设计语言

- **机核风**：冷灰基底 + 每套主题一个信号色，锐利直角（圆角 ≤2px），无辉光、无彩虹渐变
- **深浅色主题**：深色 / 浅色 / 跟随系统（`localStorage.mpTheme`），切换时冻结过渡以保证一帧内完成
- **79 个线性图标**：全部是 1.5px 描边的内联 SVG（`mask` + `currentColor`），零外链、零字体、零图片；
  HTML 与 JS 里已经没有任何 emoji
- **完整动效系统**：时长 120 / 200 / 280 / 420 / 560ms 五档 token + 5 条指数缓动曲线；
  只动 `transform` / `opacity`（按下反馈与进度条是两处显式例外）
- **无障碍**：尊重 `prefers-reduced-motion`（关掉位移与循环动画、保留颜色反馈与功能性动画），
  全站键盘可达（侧栏折叠头、菜单项、弹窗都是原生可聚焦元素）

## 技术栈

- 纯原生 ES modules，没有 npm、打包器、框架、CDN
- Web Audio API 实时合成、卷积混响、离线渲染导出 WAV
- 纯 JS 实现 SMF (MIDI) 读写、WAV 编码、LZSS 压缩分享链接、撤销/重做快照
- 46 个模块，分层是 `ui → io/ai/audio → core`，没有循环依赖
- 时间线虚拟滚动：长曲下只渲染可见区域的列，节点数从 17 万降到 2 万以内

### 本地运行

在项目根目录执行：

    python -m http.server 8080

或者：

    npx serve -p 8080

然后浏览器打开 http://localhost:8080

### 项目结构

    index.html            只放 DOM 骨架，入口指向 ./js/main.js
    visualizer.html       可视化页骨架，入口指向 ./js/viz/main.js
    css/                  主题变量、布局、时间线、可视化页样式
    js/main.js            入口：import + 错误监听 + hooks 注入 + boot()
    js/core/              util、theory、state、storage
    js/audio/             engine、synth、drum、master、render
    js/ai/                styles、rhythm、harmony、lead、compose
    js/io/                project、share、midi、wav
    js/ui/                timeline、sidebar、seek、piano、viz、toolbar、theme
    js/viz/               可视化页：main、audio、analyzer、transport、features、registry、cover、fingerprint、shareCard、renderers/（8 个）
    assets/favicon.svg    站点图标

## 更新日志

### [1.1.0] - 2026-09-18

**新增**

- 🎨 音乐可视化独立页面
  - 8 个实时渲染器：实时波形 / 频谱瀑布 / 心电图 / 雷达图 / 径向频谱 / 粒子系统 / 跳动波形 / 3D 频谱森林
  - 3 种图片生成器：专辑封面（3 风格）/ 音乐指纹 / 分享卡片，都支持 2× 高清导出
- 分享卡片生成（1200×630，含波形 + BPM/调式/音符数 + 链接）
- 双主题系统（深色 / 浅色 / 跟随系统），切换耗时从 515ms 降到 136–162ms（128 小节下）
- 全站 79 个线性图标，HTML 与 JS 里的 emoji 全部清零
- 完整动效系统：时长 / 缓动 token 化 + 10 项动效（面板切换、菜单、弹窗分层、tab 滑块、数字 tick、
  按下反馈、加载态、列表交错、主题脉动、粒子呼吸）
- 主应用 ↔ 可视化页 View Transitions 跨页过渡（含 logo 共享元素飞行）
- 主应用侧栏低频块折叠（4 块，状态记入 `localStorage.mpSidebarCollapsed`）
- 主应用 stageTop「编辑 ▾」菜单（收纳人性化力度 / 随机音色 / 清空选中轨 / 跟随 4 项低频操作）
- 可视化页：参数面板常用/更多分级、渲染长度收进齿轮菜单、FPS 按需显示

**优化**

- 渲染 ETA：学习历史速度（`localStorage.vizRenderSpeed`）+ 每 500ms 校正 + 数字单调递减
- 可视化页顶栏从 12 项降到 9 项（时间合并、取消按需出现、返回收成图标）
- 位置 / 时长 / BPM 三处信息合并，各自只在一处显示

**修复**

- 暂停后播放归零 + 双重播放指针
- 时间线虚拟滚动正确性
- 3D 森林在 yaw=90° 时有 0.6%（21/3691）柱子出现约 2px² 的亚像素剪影缺口
- ETA 末段反弹（从"还需 6 秒"弹回"还需 15 秒"）→ 改为单调递减 + 末期改文案

**已知限制**

- 跨页过渡依赖浏览器支持（Chromium 126+ 的跨文档 View Transitions）与同源 HTTP/HTTPS：
  本地 `file://` 打开、或部分 Edge 版本可能被禁用，届时退化为直接跳转（功能不受影响）
- 原生 `<select>` 的展开动画由系统绘制，只能给外层加点击微反馈
- 3D 森林在极端角度有亚像素级剪影残留（肉眼不可见）

## 授权

个人项目，自由使用。有问题或建议可以提 Issue。
